/** Generic visual tone used by status badges and timeline severities across every domain (ideas, projects, notifications...). */
export type StatusTone = 'success' | 'warning' | 'danger' | 'info' | 'neutral';
